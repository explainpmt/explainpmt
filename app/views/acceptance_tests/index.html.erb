<div class="notice info">
	<p> Acceptance testing is used to determine behavioral intent for a deliverable and provides formal completion criteria for a deliverable.</p>
</div>
<div class="box_top">
	<h2>Acceptance Tests</h2>
	<ul class="sorting">
		<li><%= link_to_new_acceptance_test %></li>
		<li><%= link_to_export_acceptance %></li>
	</ul>
</div>
<% if @acceptance_tests.present? %>
<div class="box_content">
	<%= form_tag assign_project_acceptance_tests_path(current_project) %>
		<div class="move_to">
			Move Selected Acceptance Tests to:
			<select name="move_to">
				<option value="0">Not Assigned</option>
				<%= story_select_list_for(current_project.stories) %>
			</select>
			<%= submit_tag "Go" %>
		</div>
		<%= render :partial => 'acceptance_tests' %>
	</form>
</div>
<% else %>
<div class="box_content padding">
	<p>This project does not have any Acceptance Tests yet.</p>
</div>
<% end %>