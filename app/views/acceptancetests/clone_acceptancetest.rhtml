<%= form_tag(:controller => 'acceptancetests', :action => 'create',
				   :id => @object.id,
                   :project_id => @project.id) %>
<%= render_partial('acceptancetest_form') %>
</form>