<%= form_tag(:controller => 'acceptancetests', :action => 'update',
				   :id => @acceptancetest.id,
                   :project_id => @project.id) %>
<%= render_partial('acceptancetest_form') %>
</form>
