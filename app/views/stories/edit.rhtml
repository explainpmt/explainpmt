<h3>Edit story card</h3>

<%= form_tag(:controller => 'stories', :action => 'update',
                   :id => @story.id, :project_id => @project.id) %>
<%= render_partial('story_form', :story => @story) %>
</form>

