<h3>Create new story card</h3>

<%= start_form_tag(:controller => 'stories', :action => 'create',
                   :project_id => @project.id) %>
<%= render_partial('story_form', :story => @story) %>
<%= end_form_tag %>
